智能浮力控制装置 (Auto-BCD) 数值仿真计算需求与验证规范1. 核心证明需求 (Requirements to Prove)在编写仿真代码时，程序必须能够通过计算结果从数学和物理角度证明以下四个核心工程结论：需求 1.1：证明系统的“正反馈失稳”物理特性验证目标：证明在无主动控制（气阀关闭）的情况下，微小的深度扰动会导致潜水员加速下坠或加速上升，系统不具备自恢复能力。需求 1.2：证明 RK4 算法在强非线性系统中的降代/保真优势验证目标：证明在相同的时间步长（$\Delta t$）下，显式欧拉法（Euler）会迅速发散或产生极大误差，而四阶龙格-库塔法（RK4）能够保持高精度稳定求解。需求 1.3：证明牛顿迭代法在静态配平中的高效性验证目标：证明在任意合理的目标深度下，牛顿迭代法能在极少步数（通常 < 5 步）内收敛到中性浮力所需的目标气体质量，满足嵌入式系统的实时性要求。需求 1.4：证明最小二乘法对材质非线性的逼近有效性验证目标：证明通过多项式拟合后的潜水服体积变化曲线，能够平滑且高精度地穿过（或逼近）高压舱测得的离散实验数据点。2. 计算过程与关键公式 (Process & Key Formulas)阶段一：潜水服受压形变的参数拟合（数据准备）过程：读取离散的实验数据点 $(y_i, V_{suit,i})$，利用最小二乘法构建正规方程组，求解多项式系数，得到平滑的体积-深度函数。关键公式（正规方程组）：$$\mathbf{A}^T \mathbf{A} \mathbf{a} = \mathbf{A}^T \mathbf{b}$$$$V_{suit}(y) = a_0 + a_1 y + a_2 y^2$$阶段二：计算悬浮前馈基准量（静态求解）过程：接收用户输入的目标深度 $y_{target}$，将其代入静力学平衡方程，使用牛顿迭代法计算出维持该深度中性浮力所需的最优气体质量 $m_{air}^*$。关键公式（非线性求根与牛顿迭代格式）：$$F(m_{air}) = m_{total} g - \rho g \left[ V_{body} + V_{suit}(y_{target}) + \frac{m_{air} R T}{P_0 + \rho g y_{target}} \right] = 0$$$$m_{air}^{(k+1)} = m_{air}^{(k)} - \frac{F(m_{air}^{(k)})}{F'(m_{air}^{(k)})}$$阶段三：垂直动力学时域计算（动态仿真）过程：设定初始状态向量 $\mathbf{X}_0 = [y_0, v_0]^T$，设定时间步长 $\Delta t$。在一个 for/while 循环中，将系统的非线性微分方程转化为一阶状态空间形式，利用 RK4 算法进行步进积分，记录每一步的深度、速度和加速度。关键公式（状态空间方程）：$$\frac{d\mathbf{X}}{dt} = \mathbf{f}(t, \mathbf{X}) = \begin{bmatrix} v \\ g - \frac{\rho g}{m_{total}}\left( V_{body} + V_{suit}(y) + \frac{m_{air}RT}{P_0 + \rho gy} \right) - \frac{\rho C_d A}{2 m_{total}}v|v| \end{bmatrix}$$关键公式（RK4递推）：$$\mathbf{k}_1 = \mathbf{f}(t_n, \mathbf{X}_n)$$$$\mathbf{k}_2 = \mathbf{f}(t_n + \frac{\Delta t}{2}, \mathbf{X}_n + \frac{\Delta t}{2}\mathbf{k}_1)$$$$\mathbf{k}_3 = \mathbf{f}(t_n + \frac{\Delta t}{2}, \mathbf{X}_n + \frac{\Delta t}{2}\mathbf{k}_2)$$$$\mathbf{k}_4 = \mathbf{f}(t_n + \Delta t, \mathbf{X}_n + \Delta t\mathbf{k}_3)$$$$\mathbf{X}_{n+1} = \mathbf{X}_n + \frac{\Delta t}{6}(\mathbf{k}_1 + 2\mathbf{k}_2 + 2\mathbf{k}_3 + \mathbf{k}_4)$$3. 输出结果与可视化图表要求 (Output Visualizations)程序的最终输出必须包含以下四个关键的可视化图表，以直观支撑前文提出的证明需求：图表 1：无控制状态下的系统失稳相图 (证明需求 1.1)图表类型：双 Y 轴折线图 (Line Chart)X 轴：时间 $t$ (秒)左 Y 轴：当前深度 $y$ (米) —— 建议 Y 轴反转，0 在上方，模拟真实水下视角。右 Y 轴：垂直速度 $v$ (米/秒)预期表现：在给定一个微小的初始向下速度后，深度曲线呈指数级下坠趋势，速度绝对值迅速增大直至达到终端速度（受流体阻力限制），证明波义耳定律导致的正反馈失稳。图表 2：欧拉法 vs RK4 法的积分漂移对比 (证明需求 1.2)图表类型：多线对比折线图 (Multi-line Chart)X 轴：时间 $t$ (秒)Y 轴：绝对深度误差 $|y_{euler} - y_{rk4}|$ 或当前深度 $y$预期表现：在相同的较大步长（如 $\Delta t = 0.1s$）下，绘制两条轨迹。可以明显观察到欧拉法的曲线率先发生振荡或发散，而 RK4 曲线保持平滑，证明高阶数值分析方法在强非线性场景下的必要性。图表 3：牛顿迭代法收敛效率图 (证明需求 1.3)图表类型：散点连线图 / 阶梯图 (Step Line Chart)X 轴：迭代次数 $k$ (次第: 1, 2, 3...)Y 轴：计算得出的目标气体质量 $m_{air}^{(k)}$ 与目标函数残差 $F(m_{air})$预期表现：目标气体质量在 3-5 次迭代后迅速稳定在一条水平直线上，同时残差 $F$ 的数值呈对数级下降至接近 0（如 $10^{-6}$ 量级），证明算法的高效收敛。图表 4：潜水服材质拟合与离散数据对比 (证明需求 1.4)图表类型：散点+平滑曲线组合图 (Scatter + Smooth Line Plot)X 轴：深度 $y$ (米)Y 轴：潜水服排开水的体积 $V_{suit}$ (升)预期表现：图表上显示一组离散的实验测量点（散点），以及一条贯穿这些点的平滑二次或三次多项式曲线（最小二乘法结果），体现理论对经验数据的逼近。